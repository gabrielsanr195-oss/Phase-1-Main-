// Admin Dashboard — Control Tower, Event Config, CRM, Reports
// Built in Phase 1+ alongside each feature sprint
export default function App() {
  return (
    <div style={{ fontFamily: 'sans-serif', padding: '2rem' }}>
      <h1>Phase 1+ — Admin Dashboard</h1>
      <p>Dashboard screens built progressively per sprint.</p>
    </div>
  );
}
